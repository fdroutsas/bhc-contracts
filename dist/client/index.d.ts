/**
 * BitHosting contracts — generated TypeScript surface (types only, zero
 * runtime). Regenerated EXCLUSIVELY here (`npm run generate:client`);
 * consumers install this repo as a git dependency pinned to a SHA and
 * create their runtime with openapi-fetch:
 *
 *   import createClient from 'openapi-fetch';
 *   import type { paths } from '@bithosting/bhc-contracts';
 *   const client = createClient<paths>({ baseUrl: '/api/v1' });
 */
export type { paths, components, operations } from './schema';
